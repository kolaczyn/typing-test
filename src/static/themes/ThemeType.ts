type ThemeType = {
  palette: {
    box: string;
    border: string;
    background: string;
    text: string;

    green: string;
    blue: string;
    red: string;
    yellow: string;
    lightGray: string;
    darkGray?: string;
  };
  themeName: string;
};
export default ThemeType;
